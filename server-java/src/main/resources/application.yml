server:
  port: 2345

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/renovation_db
    username: ybogdanq
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet
